<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.EpicToDo"
        tools:targetApi="31">

        <!--登录Activity-->
        <activity
            android:name=".login.common.LoginingActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--实际登录页面-->
        <activity
            android:name=".login.common.LoginActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".login.common.LoginProblemActivity"
            android:exported="true">

        </activity>

        <!--注册-->
        <activity android:name=".login.register.SignInPhone" android:exported="true" />
        <activity android:name=".login.register.SignInPassword" android:exported="true"/>

        <!--忘记密码-->
        <activity android:name=".login.forgot.ForgotPhoneActivity" android:exported="true"/>
        <activity android:name=".login.forgot.ForgotPasswordActivity" android:exported="true"/>

        <!--主页Activity-->
        <activity
            android:name=".home.HomeActivity"
            android:exported="true"
            android:launchMode="singleTask">

        </activity>

        <!--统计页面Activity-->
        <activity
            android:name=".epic.statistics.StatisticsActivity"
            android:exported="true">

        </activity>

        <!--设置页面-->
        <activity
            android:name=".mine.setting.SettingActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>

    </application>

</manifest>